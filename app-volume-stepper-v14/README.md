# App Volume Stepper v1.5

Strict Samsung Sound Assistant per-app slider detection. The overlay requires five aligned system sliders plus a separate right-side percentage slider, hides when that slider is absent, and avoids coordinate-tap volume jumps.
