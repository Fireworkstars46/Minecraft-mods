# App Volume Stepper v1.4

Rebuilt accessibility overlay for Samsung Sound Assistant. It only appears for separate per-app sliders, remembers its movable position, and converts Samsung's raw range into 0–100% steps.

Build retry with compiler-log capture enabled.
